import React from "react";
import "../styles/above_form.sass"

const AboveForm = (props) => (
    <div className={'above_form'}>
        <img src={require('../../../assets/logo2.png')} />
        <h2>red internacional de asistentes terapéuticos</h2>
    </div>
)

export default AboveForm
